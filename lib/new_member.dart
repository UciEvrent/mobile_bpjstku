import 'package:flutter/material.dart';

class newMember extends StatelessWidget {
  const newMember({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pendaftaran Anggota Baru BPJSTKU 2021"),
      ),
    );
  }
}